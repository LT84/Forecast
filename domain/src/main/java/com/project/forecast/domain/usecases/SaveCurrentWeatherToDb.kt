package com.project.forecast.domain.usecases

import com.project.forecast.domain.repository.WeatherRepository

class SaveCurrentWeatherToDb(private val weatherRepository: WeatherRepository) {

    fun execute(param: WeatherRepository) {
    }

}